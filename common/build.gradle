plugins {
    id "multiloader-common"
    id "org.relativitymc.neo-loom"
}

dependencies {
    minecraft "com.mojang:minecraft:${project.minecraft_version}"

    // Fabric and NeoForge both bundle Fabric Mixin, so it is safe to use it in common
    // If you need to update, check what version they are using to see what is compatible
    // https://github.com/neoforged/NeoForge/blob/26.1.x/gradle.properties#L37
    // https://github.com/FabricMC/fabric-loader/blob/master/gradle.properties#L12
    //compileOnly('net.fabricmc:sponge-mixin:0.17.3+mixin.0.8.7')
    annotationProcessor compileOnly("io.github.llamalad7:mixinextras-common:${mixin_extras_version}")
    annotationProcessor implementation("io.wispforest:owo-lib:${owo_lib_version}") {
        exclude "group": "net.fabricmc.fabric-api"
    }
}

configurations {
    commonJava {
        canBeResolved = false
        canBeConsumed = true
    }
    commonResources {
        canBeResolved = false
        canBeConsumed = true
    }
}

loom {
    accessWidenerPath = file("src/main/resources/alloy-forgery.accesswidener")
}
artifacts {
    commonJava sourceSets.main.java.sourceDirectories.singleFile
    commonResources sourceSets.main.resources.sourceDirectories.singleFile
}

test {
    failOnNoDiscoveredTests = false
}